{
  "name": "2026-strategy",
  "version": "0.3.1",
  "description": "Strategic Initiative Planning Tool",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "shutdown": "fuser -k -s 3001/tcp || true && fuser -k -s 5173/tcp || true && echo 'Shutdown complete: Ports 3001 and 5173 cleared.'",
    "dev": "concurrently \"npm:dev:*\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
